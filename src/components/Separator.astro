---
export const partial = true;

interface Props {
    visible?: boolean
}

const { visible } = Astro.props;
---
 
{ (visible ?? true) && <hr/> }

<style>
    hr {
        width: 150px;
        border: 0.5px solid #313338;
        margin: 0px auto 0px auto;
    }
</style>